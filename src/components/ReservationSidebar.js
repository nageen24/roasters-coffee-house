'use client';
// This component is now replaced by ReservationModal in page.js
// It renders nothing - the modal is handled directly in each page's context
export default function ReservationSidebar() {
  return null;
}
